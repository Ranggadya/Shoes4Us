const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const userId = '3d686cb5-1841-40ac-ad04-b417b0e61ed0'; const cart = await prisma.cart.findUnique({ where: { userId }, include: { items: { include: { product: true } } } }); console.log('Cart Items Length:', cart.items.length); } main().finally(() => prisma.$disconnect());
