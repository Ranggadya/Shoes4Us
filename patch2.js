const fs = require('fs'); const file = 'src/layers/services/OrderService.ts'; let content = fs.readFileSync(file, 'utf8'); content = content.replace('if (!cart || cart.items.length === 0) {', 'console.log([DEBUG] checkout called for userId: ); console.log([DEBUG] cart from DB:, JSON.stringify(cart, null, 2)); if (!cart || cart.items.length === 0) {'); fs.writeFileSync(file, content);
